char* _strcat(char* dest, const char* src) {
    char* p = dest + strlen(dest); // find the end of dest
    while (*src != '\0') { // copy the characters from src to dest
        *p++ = *src++;
    }
    *p = '\0'; // add the null terminator to dest
    return dest;
}
